# TMEM Linear Generalization Handoff (2026-04-09)

## Repo State
- Repo: `/root/code/triton-tmem-isolated`
- Branch: `codex/tmem-isolated`
- HEAD: `bdd6ffa2c` (`Add initiative handoff discipline for TMEM debugging`)
- Remote: `jeffniu-openai/codex/tmem`
- Worktree: dirty

Dirty files at this checkpoint candidate:
- `.codex/initiatives/tmem_linear_generalization/handoff_2026-04-09.md`
- `.codex/initiatives/tmem_linear_generalization/log.md`
- `.codex/initiatives/tmem_linear_generalization/memory.md`
- `lib/Dialect/TritonNvidiaGPU/IR/Dialect.cpp`
- `lib/Dialect/TritonNvidiaGPU/IR/TensorMemoryUtils.cpp`
- `python/src/gluon_ir.cc`
- `third_party/nvidia/lib/TritonNVIDIAGPUToLLVM/TensorMemoryToLLVM.cpp`

## Initiative State
- Source of truth remains the legacy initiative bundle:
  - `.codex/initiatives/tmem_linear_generalization/memory.md`
  - `.codex/initiatives/tmem_linear_generalization/log.md`
- The merge-base hack cleanup backlog is closed.
- The previous live one-CTA tcgen05 root-load bug is fixed in the dirty tree.
- The next step after checkpointing is to rerun the broader healthy-node 4-GPU sweep from this cleaner state and classify any remaining failures.

## Fixed Bug
Exact repro that is now green:
- `python/test/gluon/test_core.py::test_mma_shared_inputs[False-ctas_per_cga0-1-1-1-64-0-32-warps2-8-False-True-acc_dtype0]`

Key nearby controls that are also green:
- `python/test/gluon/test_core.py::test_block_m_64_mma[linear]`
- `python/test/gluon/test_core.py::test_tmem_linear_roundtrip_splitn_shapes[linear_m64_splitn_64x32-layout11-64-32-expected_offset_imms11]`
- the 615-case row/col runtime slice spanning:
  - `test_tmem_runtime_matrix_splitn_auto_selects_16x32bx2`
  - `test_tmem_runtime_matrix_splitn_rowcol_permuted_layout_sweep`
  - `test_tmem_runtime_matrix_ldst_rowcol_permuted_layout_sweep`
  - `test_tmem_runtime_matrix_ldst_descriptor_compositions_rowcol_permuted_layout_sweep`

## Root Cause
- The bug was not another packet-offset problem.
- For rank-2 `M=64`, `f32`, 4-warp root loads, the handle-aware TMEM memdesc path could satisfy a split-N request with a speculative `I32x32b` candidate before the correct `I16x32bx2` family was considered.
- Two things caused that:
  - `python/src/gluon_ir.cc` treated `I32x32b` and `I16x32bx2` as interchangeable in `matchesDesiredAtom(...)` for rank-2 `M=64` `f32` descriptors.
  - the handle-aware candidate order prefixed row-plan-specific direct layouts ahead of the generic `getTmemCompatibleLayouts(queryTy, numWarps)` order.
- The public descriptor API already knew the correct split-N family for the same root descriptor, so this was a selection bug, not an ISA limitation.

## Structural Fix
- `python/src/gluon_ir.cc`
  - restore strict atom matching for `I32x32b` vs `I16x32bx2`;
  - align handle-aware candidate ranking with `getTmemCompatibleLayouts(queryTy, numWarps)` before appending memdesc-specific rescue layouts; and
  - try the canonical `M=64` split-N TMEM-linear type first in the public descriptor API for the relevant 4-warp `f32` cases.
- `lib/Dialect/TritonNvidiaGPU/IR/Dialect.cpp`
  - factor a reusable canonical `M=64` split-N linear layout builder; and
  - inject that canonical split-N layout into direct-layout construction and compatible-layout enumeration.
- `third_party/nvidia/lib/TritonNVIDIAGPUToLLVM/TensorMemoryToLLVM.cpp`
  - keep query-type lowering ahead of raw-query for the same `M=64` root-load family so LLVM follows the same structural choice as Gluon.
- `lib/Dialect/TritonNvidiaGPU/IR/TensorMemoryUtils.cpp`
  - keep the earlier validated row-anchor/direct-view fixes that were already supporting the broader row/col runtime frontier.

## Concrete Evidence
- Before the fix, the actual TTGIR root load used a generic linear result layout and PTX scalarized into thirty-two `tcgen05.ld.sync.aligned.32x32b.x1.b32` messages.
- After the fix, the TTGIR root load uses the split-N family:
  - `register [[0,1],[0,2],[0,4],[0,16]]`
  - `lane [[1,0],[2,0],[4,0],[8,0],[0,8]]`
  - `warp [[16,0],[32,0]]`
- PTX now uses `tcgen05.ld.sync.aligned.16x32bx2.x8.b32` for that root load.

## Validation In This Context
- `make -j8`
  - passed
- `CUDA_VISIBLE_DEVICES=0 TRITON_CACHE_DIR=/tmp/triton-cache-mma-exact-fix-clean PYTHONPATH=python:. pytest -s --tb=short -vv python/test/gluon/test_core.py::test_mma_shared_inputs[False-ctas_per_cga0-1-1-1-64-0-32-warps2-8-False-True-acc_dtype0]`
  - `1 passed`
- `CUDA_VISIBLE_DEVICES=1 TRITON_CACHE_DIR=/tmp/triton-cache-blockm64-fix-clean PYTHONPATH=python:. pytest -s --tb=short -vv python/test/gluon/test_core.py::test_block_m_64_mma[linear]`
  - `1 passed`
- `CUDA_VISIBLE_DEVICES=2 TRITON_CACHE_DIR=/tmp/triton-cache-roundtrip-fix-clean PYTHONPATH=python:. pytest -s --tb=short -vv python/test/gluon/test_core.py::test_tmem_linear_roundtrip_splitn_shapes[linear_m64_splitn_64x32-layout11-64-32-expected_offset_imms11]`
  - `1 passed`
- `CUDA_VISIBLE_DEVICES=3 TRITON_CACHE_DIR=/tmp/triton-cache-tmem-rowcol-fix PYTHONPATH=python:. pytest -s --tb=short -vv python/test/gluon/test_tmem_runtime_matrix.py::test_tmem_runtime_matrix_splitn_auto_selects_16x32bx2 python/test/gluon/test_tmem_runtime_matrix.py::test_tmem_runtime_matrix_splitn_rowcol_permuted_layout_sweep python/test/gluon/test_tmem_runtime_matrix.py::test_tmem_runtime_matrix_ldst_rowcol_permuted_layout_sweep python/test/gluon/test_tmem_runtime_matrix.py::test_tmem_runtime_matrix_ldst_descriptor_compositions_rowcol_permuted_layout_sweep`
  - `615 passed in 378.72s`

## Validation Gap
- Compiler lit checks are still unavailable on this node:
  - `lit` not on `PATH`
  - `python3 -m lit` missing
  - no local `llvm-lit` found in the build tree

## Recommended Next Steps
1. Commit and push this checkpoint with the updated initiative docs.
2. Resume the broader healthy-node 4-GPU `python/test/gluon/test_core.py` + `python/test/gluon/test_tmem_runtime_matrix.py` sweep from this cleaner tip.
3. If new failures appear, classify them by exact nodeid before editing code:
   - stale negative or stale diagnostic expectation -> update tests
   - real compiler/runtime bug -> root-cause it structurally before changing lowering again
4. If the next failure surface is compiler-only, rerun the relevant lit checks on a node with `lit` or `llvm-lit` installed.

## Session Discipline
- Keep updating this handoff file after each meaningful checkpoint and before any likely context rollover.
- Keep commit messages detailed enough to stand alone as handoff artifacts.
- Keep pushing each TMEM checkpoint to `jeffniu-openai/codex/tmem`.
